# Radar Puls API - Context Snapshot

Date: 2026-03-11

## Implemented State (Completed)

- Backend MVP implemented with NestJS + PostgreSQL.
- Endpoint implemented: POST /api/events/viber.
- Health endpoint implemented: GET /health.
- DTO validation implemented for payload fields: source, group, message, timestamp, device_id.
- Auth implemented with device token map via DEVICE_TOKENS_JSON.
- Deterministic error envelope implemented:
  - {"error":{"code":"...","message":"...","request_id":"..."}}
- DB layer implemented with TypeORM entity + migration for raw_events table.
- event_timestamp + received_at policy implemented.
- E2E tests implemented and previously passing (200/400/401/429/500 + burst 100/10s).
- README updated with runbook and commands.
- .gitignore updated to ignore node_modules/dist/env/log artifacts.

## Git State

- node_modules and dist were removed from tracking and pushed.
- Latest known commit flow included cleanup commit:
  - chore: remove node_modules and dist from tracking, update .gitignore
- main and origin/main were in sync at last verification.

## Recent User Request Context

User asked to continue with priority 1: local Docker development setup.
User requested WSL usage for optimization and local-only dev setup (not production).

## Docker/WSL Discovery Findings

- Only WSL distro available:
  - docker-desktop (running)
- No Ubuntu (or other user distro) installed/configured.
- Running docker CLI inside docker-desktop distro is unsupported.
- In current Git Bash terminal, docker command is not available (command not found).

## Current Blocker

Cannot execute docker/docker compose commands in current shell environment until one of these is true:

1. Docker CLI is available in current shell, or
2. A user WSL distro (e.g., Ubuntu) is installed and integrated with Docker Desktop.

## Files likely changed by user/tooling recently

- .env.example had recent edits.
- Core app files may have formatting or minor changes; should be revalidated after restart.

## Safe Resume Point

After restart, first action should be environment enablement for Docker CLI access, then proceed with Docker dev setup files and validation.
